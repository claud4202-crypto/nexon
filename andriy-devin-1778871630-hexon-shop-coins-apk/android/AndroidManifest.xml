<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.hexon.beta"
          android:versionCode="1"
          android:versionName="1.0">

    <uses-sdk android:minSdkVersion="21"
              android:targetSdkVersion="34" />

    <!-- Only used if the game ever needs to fetch from the network.
         file:///android_asset works without any permission. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Public Documents folder access. Used to write a small file
         containing the player's device ID outside the app sandbox,
         so the ID survives uninstall + reinstall.

           * API 23..28: legacy WRITE_EXTERNAL_STORAGE is required.
           * API 29+ : MediaStore APIs are used instead and need
                       no runtime permission. We cap the permission
                       request at API 28 with maxSdkVersion. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
                     android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
                     android:maxSdkVersion="28" />

    <uses-feature android:name="android.hardware.touchscreen"
                  android:required="false" />

    <application
        android:label="HEXON BETA"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Hexon"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="false"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout|smallestScreenSize|uiMode|density|fontScale"
            android:launchMode="singleTask"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
